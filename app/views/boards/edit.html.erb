<div class="container">
  <h1 class="mt-5">Editing board</h1>

  <div class="card mt-4">
    <div class="card-body">
      <%= render "form", board: @board %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "Show this board", @board, class: "btn btn-primary mr-2" %>
    <%= link_to "Back to home page", root_path, class: "btn btn-secondary" %>
  </div>
</div>
